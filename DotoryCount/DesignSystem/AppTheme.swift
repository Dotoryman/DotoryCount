import SwiftUI

enum AppTheme {
    static let background = Color(red: 0.97, green: 0.95, blue: 0.90)
    static let surface = Color.white.opacity(0.72)
    static let accent = Color(red: 0.43, green: 0.27, blue: 0.13)
    static let acorn = Color(red: 0.55, green: 0.32, blue: 0.13)
    static let acornCap = Color(red: 0.32, green: 0.20, blue: 0.10)
    static let glass = Color(red: 0.78, green: 0.90, blue: 0.90)
    static let secondaryText = Color.primary.opacity(0.62)

    static let cardCornerRadius: CGFloat = 24
    static let horizontalPadding: CGFloat = 20
}
